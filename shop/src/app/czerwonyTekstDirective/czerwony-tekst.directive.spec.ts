import { CzerwonyTekstDirective } from './czerwony-tekst.directive';

describe('CzerwonyTekstDirective', () => {
  it('should create an instance', () => {
    const directive = new CzerwonyTekstDirective();
    expect(directive).toBeTruthy();
  });
});
